# No environment variables required to run locally.
# WebSend uses WebRTC for direct peer-to-peer transfer.
# No API keys, no accounts, no cloud services needed.

# For production deployment with a custom signaling server:
# WS_PORT=3001
# HTTPS=true
